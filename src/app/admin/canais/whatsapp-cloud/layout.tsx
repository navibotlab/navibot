import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'WhatsApp Cloud API - Configuração',
};

export default function WhatsAppCloudLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 